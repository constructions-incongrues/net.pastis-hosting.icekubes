apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k10
  namespace: argo-cd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: k10
  project: system.backup
  source:
    chart: k10
    helm:
      values: |
        auth:
          basicAuth:
            enabled: true
            htpasswd: "tristan:{SHA}K+iK4HpzuMKnVlbTaxQSkD9/eLg="
        eula:
          accept: true
          company: "Club Jean-Cloude"
          email: tristan@rivoallan.net
        clusterName: Icekubes
        cluster:
          domainName: jeancloude.club
        ingress:
          create: true
          annotations:
            external-dns.alpha.kubernetes.io/hostname: k10.jeancloude.club
    repoURL: https://charts.kasten.io/
    targetRevision: 4.5.6
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
