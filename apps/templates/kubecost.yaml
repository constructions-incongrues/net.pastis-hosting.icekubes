apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubecost
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: jeancloude-club-kubecost
  project: jeancloude.club
  source:
    chart: cost-analyzer
    helm:
      values: |
        kubecostToken: dHJpc3RhbkByaXZvYWxsYW4ubmV0xm343yadf98
        ingress:
          enabled: true
          annotations:
            external-dns.alpha.kubernetes.io/hostname: kubecost.jeancloude.club
    repoURL: https://kubecost.github.io/cost-analyzer/
    targetRevision: 1.89.0-rc.2
  syncPolicy:
    # automated:
    #   prune: false
    #   selfHeal: false
    syncOptions:
      - CreateNamespace=true
