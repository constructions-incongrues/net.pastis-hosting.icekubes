apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: longhorn
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: jeancloude-club-longhorn
  project: jeancloude.club
  source:
    chart: longhorn
    helm:
      values: |
        ingress:
          enabled: true
          host: longhorn.jeancloude.club
    repoURL: https://charts.longhorn.io
    targetRevision: 1.2.3
  syncPolicy:
    # automated:
    #   prune: true
    #   selfHeal: true
    syncOptions:
      - CreateNamespace=true
